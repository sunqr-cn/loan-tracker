{
  "name": "loan-tracker-pages-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name=loan-tracker-api",
    "dev": "wrangler pages dev public"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
